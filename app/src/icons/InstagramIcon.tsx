import InstagramIcon from '@mui/icons-material/Instagram';
import { SvgIconProps } from '@mui/material';

const InstaIcon: React.FC<SvgIconProps> = (props) => <InstagramIcon {...props} />;

export default InstaIcon;