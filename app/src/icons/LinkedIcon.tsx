import LinkedInIcon from '@mui/icons-material/LinkedIn';
import { SvgIconProps } from '@mui/material';

const LinkIcon: React.FC<SvgIconProps> = (props) => <LinkedInIcon className='text-3xl' {...props} />;

export default LinkIcon;
