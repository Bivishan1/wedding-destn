// import withTM from 'next-transpile-modules'; // Import next-transpile-modules
// import { NextConfig } from 'next'; // Import NextConfig type

const nextConfig = {
  experimental: {},
};

// module.exports = {
//   trailingSlash: true, // Optional: Add a trailing slash to all URLs
// };

export default nextConfig;

// List the MUI packages you want to transpile
// const transpileModules = [
//   '@mui/material',
//   '@mui/system',
//   '@mui/icons-material', // Optional, only if using MUI icons
// ];

// Your Next.js config with Webpack customization
// const nextConfig: NextConfig = {
//   experimental: {
//     turbo: true, // Enable Turbopack
//   },
// };

// Wrap your configuration with next-transpile-modules
// export default withTM(transpileModules)(nextConfig);
