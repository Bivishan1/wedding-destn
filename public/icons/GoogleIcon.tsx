import GoogleIcon from '@mui/icons-material/Google';
import { SvgIconProps } from '@mui/material';

const GoogleIcn: React.FC<SvgIconProps> = (props) => <GoogleIcon {...props} />;

export default GoogleIcn;